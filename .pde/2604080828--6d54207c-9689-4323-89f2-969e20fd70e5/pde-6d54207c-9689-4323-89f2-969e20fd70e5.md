# Prompt Decomposition

> Engine: **copilot**

## Four Directions

### 🌅 EAST — Vision

- Define success criteria and taxonomy for fragments (what counts as reflection/task/issue/seed) and the desired destinations in the ecology [96%]
- Clarify role of qmd vs other routing approaches and list destination APIs available [80%] _(implicit)_

### 🔥 SOUTH — Analysis

- Research Whisper configuration, error modes for walk recordings, and alternatives for on-device or cloud transcription [90%]
- Investigate classification approaches (rule-based vs ML) and sample prompts/features to detect intentions [90%]
- Prototype qmd-based routing rules and compare to simple rule-engine [70%] _(implicit)_

### 🌊 WEST — Validation

- Validate transcription quality on sample walk recordings and measure downstream classification accuracy [90%]
- End-to-end tests that verify fragments land in correct destinations and that metadata (timestamps, origin) are preserved [88%]

### ❄️ NORTH — Action

- Implement ingestion + Whisper transcription module and store transcripts [90%]
- Build decomposition/classifier, integrate with pipeline, and implement routing connectors [90%]
- Deploy automation (cron or event-driven) and add monitoring/logging [80%] _(implicit)_

## Original Prompt

> I want to create a ceremony pipeline that takes voice notes from my walks, transcribes them, decomposes the intention, and somehow routes the fragments to the right place in my ecology. The fragments could be reflections, tasks, issues, or seeds. I assume Whisper will handle transcription and probably qmd will help with routing but I am not sure how the classification works yet.

## Primary Intent

**Action:** create
**Target:** ceremony pipeline that ingests voice notes, transcribes, decomposes intention, classifies fragments, and routes them into the user's ecology
**Urgency:** persistent
**Confidence:** 95%

## Secondary Intents

1. **transcribe** — voice notes -> text using Whisper (assumed) _(implicit)_
2. **decompose** — transcripts -> extract intentions/fragments _(explicit)_
   - depends on: transcribe
3. **classify** — fragments -> categories {reflection, task, issue, seed} _(explicit)_
   - depends on: decompose
4. **route** — classified fragments -> destination in ecology (issues, tasks, seeds, reflections) _(implicit)_
   - depends on: classify
5. **evaluate** — qmd as routing mechanism / integration approach _(implicit)_
   - depends on: classify
6. **design** — taxonomy, mapping rules, and routing policy for fragment types _(implicit)_
7. **implement** — connectors to destinations (issue tracker, task manager, notes store) _(implicit)_
   - depends on: route
8. **test** — transcription accuracy, decomposition quality, classification precision, routing correctness _(explicit)_
   - depends on: implement
9. **document** — pipeline design, operator instructions, privacy considerations _(explicit)_

## Context Requirements

### Files Needed
- pipeline.py or pipeline/ (ingest, transcribe, classify, route)
- config.yaml or config.json (routing rules, connectors)
- classifier.py or models/ (decomposition + classification)
- routing_rules.qmd or routing_rules.md
- connectors/*.py (github_issue_connector.py, task_connector.py, notes_store_connector.py)
- tests/test_pipeline.py
- README.md (usage + deployment)
- privacy_and_storage.md

### Tools Required
- Whisper (or alternative ASR)
- qmd (or QMD-based routing system) - to evaluate
- Python
- NLP libraries (spaCy, transformers or similar)
- Git / GitHub API (for issues)
- Task management API (e.g., Todoist/Asana) or local task store
- Audio utilities (ffmpeg)
- Testing framework (pytest)

### Assumptions
- Whisper will be used/available for transcription (user assumption)
- qmd may be useful for routing (user assumption)
- Fragments map to four categories: reflections, tasks, issues, seeds (user-stated)
- User's 'ecology' has programmatic endpoints for ingestion (implied but not specified)

## Expected Outputs

### Artifacts
- pipeline.py (or equivalent pipeline package)
- classifier and model artifacts under models/ or classifier.py
- routing_rules.qmd (routing spec)
- connectors/ (github_issue_connector.py, task_connector.py, notes_connector.py)
- tests/ for each stage
- config.yaml and README.md

### Updates
- README.md (project-level usage and deployment)
- docs/ with design + taxonomy
- possibly KINSHIP.md or relevant documentation describing how the pipeline fits the ecology

### Communications
- Create design PR with pipeline implementation
- Open issues for unresolved questions: classification criteria, destination APIs, privacy handling
- Add docs/ design doc and usage guide in repo

## Action Stack

- [ ] Define taxonomy and success criteria for fragment types and map to destination endpoints
- [ ] Confirm transcription tool (Whisper) configuration and install dependencies (depends on: Define taxonomy and success criteria for fragment types and map to destination endpoints)
- [ ] Implement transcription module to ingest audio and produce timestamped transcripts (depends on: Confirm transcription tool (Whisper) configuration and install dependencies)
- [ ] Design decomposition/spec (features, prompts, rules or ML labels) to extract intent fragments (depends on: Implement transcription module to ingest audio and produce timestamped transcripts)
- [ ] Implement classifier/decomposer (rule-based or ML) and unit tests (depends on: Design decomposition/spec (features, prompts, rules or ML labels) to extract intent fragments)
- [ ] Evaluate qmd for routing vs alternative rule engine and choose integration approach (depends on: Implement classifier/decomposer (rule-based or ML) and unit tests)
- [ ] Implement routing engine and connectors to each destination (issues, tasks, reflections store, seeds store) (depends on: Evaluate qmd for routing vs alternative rule engine and choose integration approach)
- [ ] Write end-to-end tests and validate routing correctness and data fidelity (depends on: Implement routing engine and connectors to each destination (issues, tasks, reflections store, seeds store))
- [ ] Document pipeline, privacy considerations, deployment steps, and open design PR (depends on: Write end-to-end tests and validate routing correctness and data fidelity)
- [ ] Deploy automation (schedule or event-driven), add logging/monitoring, iterate on classifier/routing based on feedback (depends on: Document pipeline, privacy considerations, deployment steps, and open design PR)

## Ambiguity Flags

- **"How exactly 'ecology' accepts fragments (APIs, git issues, local files, QMD collections) is unspecified"**
  - Suggestion: List specific destination endpoints and formats (e.g., GitHub issues, Todoist project ID, local notes folder, QMD collection) so connectors can be implemented
- **"User says 'I assume Whisper' and 'probably qmd' — transcription and routing choices are uncertain"**
  - Suggestion: Confirm whether Whisper must be used or alternatives are acceptable and clarify whether qmd is required or optional; run short evaluation on both options
- **"Classification approach is unspecified ('not sure how the classification works yet')"**
  - Suggestion: Decide between rule-based heuristics vs ML classifier and provide sample labeled examples or choose a human-in-the-loop starting mode
- **"Privacy/consent/storage retention for voice notes not addressed"**
  - Suggestion: Clarify retention, encryption, and sharing policies for voice data and transcripts before deployment
